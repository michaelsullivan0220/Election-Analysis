# IF statements in Python3
# always starts with IF and followed by a condition that can either be true or false
# the statements need to be indented consistency
# the below is checking if the index [1] matches "Denver". if so, it will print it
counties = ["Arapahoe","Denver","Jefferson"]
if counties[1] == 'Denver':
    print(counties[1])

